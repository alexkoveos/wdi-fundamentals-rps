<!DOCTYPE html><html>  <head>    <meta content="text/html; charset=windows-1252" http-equiv="content-type">    <title>rockpaperscissors.js</title>  </head>  <body>    <p>////////////////////////////////////////////////<br>      /*&nbsp;&nbsp; Provided Code - Please Don't Edit&nbsp;&nbsp; */<br>      ////////////////////////////////////////////////<br>      'use strict';<br>      <br>      function getInput() {<br>      &nbsp;&nbsp;&nbsp; console.log("Please choose either 'rock', 'paper', or      'scissors'.");<br>      &nbsp;&nbsp;&nbsp; return prompt();<br>      }<br>      function randomPlay() {<br>      &nbsp;&nbsp;&nbsp; var randomNumber = Math.random();<br>      &nbsp;&nbsp;&nbsp; if (randomNumber &lt; 0.33) {<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return "rock";<br>      &nbsp;&nbsp;&nbsp; } else if (randomNumber &gt; 0.66) {<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return "paper";<br>      &nbsp;&nbsp;&nbsp; } else {<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; return "scissors";<br>      &nbsp;&nbsp;&nbsp; }<br>      }<br>      ////////////////////////////////////////////////<br>      /*&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; Write Your      Code      Below&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; */<br>      ////////////////////////////////////////////////<br>      <br>      function getPlayerMove(move) {<br>      &nbsp;&nbsp;&nbsp; // Write an expression that operates on a variable      called `move`<br>      &nbsp;&nbsp;&nbsp; // If a `move` has a value, your expression should      evaluate to that value.<br>      &nbsp;&nbsp;&nbsp; // However, if `move` is not specified / is null, your      expression should equal `getInput()`.<br>      &nbsp;&nbsp;&nbsp; return move || getInput();/* Your Expression */<br>      }<br>      <br>      function getComputerMove(move) {<br>      &nbsp;&nbsp;&nbsp; // Write an expression that operates on a variable      called `move`<br>      &nbsp;&nbsp;&nbsp; // If a `move` has a value, your expression should      evaluate to that value.<br>      &nbsp;&nbsp;&nbsp; // However, if `move` is not specified / is null, your      expression should equal `randomPlay()`.<br>      &nbsp;&nbsp;&nbsp; return move || randomPlay() /* Your Expression */;<br>      }<br>      <br>      <br>      function getWinner(playerMove,computerMove) {<br>      &nbsp;&nbsp;&nbsp; var winner;<br>      &nbsp;&nbsp;&nbsp; // Write code that will set winner to either 'player',      'computer', or 'tie' based on the values of playerMove and computerMove.<br>      &nbsp;&nbsp;&nbsp; // Assume that the only values playerMove and      computerMove can have are 'rock', 'paper', and 'scissors'.<br>      &nbsp;&nbsp;&nbsp; // The rules of the game are that 'rock' beats      'scissors', 'scissors' beats 'paper', and 'paper' beats 'rock'.<br>      &nbsp;&nbsp;&nbsp; playerMove = getPlayerMove();<br>      &nbsp;&nbsp;&nbsp; computerMove = getComputerMove();<br>      &nbsp;&nbsp;&nbsp; console.log(playerMove);<br>      &nbsp;&nbsp;&nbsp; console.log(computerMove);<br>      &nbsp;&nbsp;&nbsp; if ( playerMove === "rock" &amp;&amp; computerMove ===      "paper"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; winner = "computer";<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp;&nbsp; else if ( playerMove === "paper" &amp;&amp;      computerMove === "scissors"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; winner = "computer";<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp;&nbsp; else if ( playerMove === "scissors" &amp;&amp;      computerMove === "rock"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; winner = "computer";<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp;&nbsp; else if ( playerMove === "rock" &amp;&amp;      computerMove === "scissors"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; winner = "player";<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp; else if ( playerMove === "paper" &amp;&amp;      computerMove === "rock"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; winner = "player";<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp; else if ( playerMove === "scissors" &amp;&amp;      computerMove === "paper"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; winner = "player";<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp; else if ( playerMove === computerMove) {<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; winner = "tie";}<br>      &nbsp;&nbsp;&nbsp; console.log(winner);<br>      &nbsp;&nbsp; return winner;<br>      }<br>      <br>      function playToFive() {<br>      &nbsp;&nbsp;&nbsp; console.log("Let's play Rock, Paper, Scissors");<br>      &nbsp;&nbsp;&nbsp; var playerWins = 0;<br>      &nbsp;&nbsp;&nbsp; var computerWins = 0;<br>      &nbsp;&nbsp;&nbsp; // Write code that plays 'Rock, Paper, Scissors' until      either the player or the computer has won five times.<br>      &nbsp;&nbsp;&nbsp; while (playerWins &lt; 6 &amp;&amp; computerWins &lt;      6){<br>      &nbsp;&nbsp;&nbsp; <br>      &nbsp;&nbsp;&nbsp; var win = getWinner(null, null);<br>      &nbsp;&nbsp;&nbsp; <br>      &nbsp;&nbsp;&nbsp; if (win === "player"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; playerWins ++;<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp; if (win === "computer"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; computerWins ++;<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp; if (win === "tie"){<br>      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; null;<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp; console.log(playerWins);<br>      &nbsp;&nbsp;&nbsp; console.log(computerWins);<br>      &nbsp;&nbsp;&nbsp; }<br>      &nbsp;&nbsp;&nbsp; return [playerWins, computerWins];<br>      &nbsp;&nbsp;&nbsp; }<br>      playToFive();</p>  </body></html>